apiVersion: v1
kind: Service

metadata:
  name: whisper-service
  namespace: default

  labels:
    app: whisper-api

spec:
  selector:
    app: whisper-api

  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000

  type: LoadBalancer